# anthrie-website

Generated output. **Do not edit this repository by hand.**

Every file here is built from the private source repo `Intellumia/anthrie`
(`site/`) and published by `.github/workflows/deploy-site.yml`. Publishing
replaces this repository's entire tree, so any edit made directly here is
lost at the next deploy — including this file, which the build re-emits.

To change the site, change `site/` in the source repo and merge to `main`.
