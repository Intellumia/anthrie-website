# anthrie-website

Source for [anthrie.com](https://anthrie.com), published via GitHub Pages
directly from this repository's `main` branch.

Plain static HTML/CSS — no build step, no framework. Edit `index.html` (or
the files under `privacy/`) directly and push to `main` to publish.
